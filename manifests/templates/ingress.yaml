---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: final                  # Ingress name
  labels:
    app: final
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "final.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: final               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
